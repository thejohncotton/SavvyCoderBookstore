{
  "name": "savvy-bookstore",
  "version": "1.0.0",
  "description": "A bookstore hackathon project",
  "main": "index.js",
  "scripts": {
    "build": "parcel build index.html && cp _redirects ./dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "John Cotton",
  "license": "MIT",
  "dependencies": {
    "parcel": "^1.10.3"
  }
}
